# Reloop SMTP - Go (`net/smtp`) Example

Send emails using Reloop SMTP relay with Go standard library `net/smtp`.

## Setup & Usage

1. Copy `.env.example` to `.env` and set your Reloop API key:
   ```bash
   cp .env.example .env
   ```

2. Run the example:
   ```bash
   go run main.go
   ```
