# Reloop SMTP - Rust (Lettre) Example

Send emails using Reloop SMTP relay with Lettre in Rust.

## Setup & Usage

1. Copy `.env.example` to `.env` and set your Reloop API key:
   ```bash
   cp .env.example .env
   ```

2. Run the example:
   ```bash
   cargo run
   ```
